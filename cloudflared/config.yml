# Cloudflare Named Tunnel config for Volta
# tunnel ID and credentials-file are filled after running: cloudflared tunnel create volta

tunnel: TUNNEL_ID_HERE
credentials-file: /etc/cloudflared/TUNNEL_ID_HERE.json

ingress:
  - hostname: volta.kore29.com
    service: http://frontend:3000
  - service: http_status:404
