## 1. Create Button Component

- [x] 1.1 Implement `<Button />` in `frontend/components/ui/volta-ui.tsx` with standard size/variant styles using `font-medium` weight

## 2. Refactor Modal Form Actions

- [x] 2.1 Refactor buttons in `AddClientModal.tsx` to use `<Button />`
- [x] 2.2 Refactor buttons in `AddServiceModal.tsx` to use `<Button />`
- [x] 2.3 Refactor buttons in `NewAppointmentModal.tsx` to use `<Button />`
- [x] 2.4 Refactor modal close (X) buttons in all three modals to use `<Button />`

## 3. Refactor Main Views and Layouts

- [x] 3.1 Refactor the navigation CTA in `Sidebar.tsx` to use `<Button />`
- [x] 3.2 Refactor search, action, and table buttons in `clientes/page.tsx` to use `<Button />`
- [x] 3.3 Refactor navigation, today, and calendar view switches in `inicio/page.tsx` to use `<Button />`
- [x] 3.4 Refactor buttons in `ajustes/page.tsx` to use `<Button />`
- [x] 3.5 Refactor buttons in `sedes/page.tsx` to use `<Button />`
- [x] 3.6 Refactor buttons in `login/page.tsx` to use `<Button />`
- [x] 3.7 Refactor buttons in `lopd/[id]/page.tsx` to use `<Button />`
- [x] 3.8 Refactor buttons in `error.tsx`, `not-found.tsx`, and `admin/page.tsx` to use `<Button />`
- [x] 3.9 Refactor buttons in `Header.tsx` to use `<Button />`

## 4. Verification

- [x] 4.1 Run development build to verify visual appeal and ensure no type compilation or import errors exist
