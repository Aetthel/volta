## 1. Layout Refactoring

- [x] 1.1 Remove the two-column split-pane structure in `frontend/app/login/page.tsx` and center the login container.
- [x] 1.2 Implement the brand logo icon and title heading at the top of the centered container.

## 2. Core Login Form & Fields

- [x] 2.1 Refactor form inputs (Email and Password) to use clean borders and placeholder styling, preserving the password visibility toggle.
- [x] 2.2 Re-layout the submit button to use the primary `Button` component from `volta-ui.tsx`.
- [x] 2.3 Align the "¿Olvidaste tu contraseña?" option cleanly under the input fields.

## 3. Secondary Actions & Footer

- [x] 3.1 Implement thin outline-bordered social login buttons for Google and Apple side-by-side with standard SVG icons.
- [x] 3.2 Place a secondary outlined button for creating a new account ("Crear Cuenta Nueva") at the bottom of the card flow.
- [x] 3.3 Add the small disclaimer notice at the very bottom of the page.
- [x] 3.4 Verify compilation and responsiveness across all resolutions.
