## 1. Global Core Colors Sincronizacion

- [x] 1.1 Update `--color-primary` to `#006565` and `--color-primary-container` to `#008080` in `frontend/app/globals.css`

## 2. Primitive UI Components Border-Radius Alignment

- [x] 2.1 Update input components (`FloatingInput`, `Select`, `Textarea`) in `frontend/components/ui/volta-ui.tsx` to use `rounded-sm` instead of `rounded-md`
- [x] 2.2 Update `Card` component in `frontend/components/ui/volta-ui.tsx` to use `rounded-default` instead of `rounded-2xl`

## 3. Custom Component Colors Clean-up

- [x] 3.1 Replace hardcoded hex colors and slate classes with variables in `frontend/components/MetricCard.tsx`
- [x] 3.2 Replace hardcoded hex colors and slate classes with variables in `frontend/components/WeeklyPerformanceChart.tsx`
- [x] 3.3 Replace hardcoded hex colors and slate classes with variables in `frontend/components/FeaturedServicesList.tsx`
- [x] 3.4 Replace hardcoded hex colors and slate classes with variables in `frontend/components/UpcomingAppointmentsList.tsx`

## 4. Modal Toast Standardisation

- [x] 4.1 Update the success toast notification layout and colors in `frontend/components/NewAppointmentModal.tsx`
