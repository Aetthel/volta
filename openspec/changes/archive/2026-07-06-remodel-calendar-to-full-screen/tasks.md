## 1. Clean Layout Remodeling

- [x] 1.1 Remove `<PageHeader>` and outer `<Card>` component in `frontend/app/agenda/page.tsx` and change `<main>` container classes to make it full bleed.

## 2. Dynamic Stylists Load & Dropdown Filter

- [x] 2.1 Fetch workers from `/api/backend/users` in `frontend/app/agenda/page.tsx`, assign stylists to mapped appointments, and introduce the stylist dropdown filter in the controls header.

## 3. Custom Controls and Time indicator updates

- [x] 3.1 Update navigation controls to show week ranges next to header month title with outline Hoy button.
- [x] 3.2 Implement a global current time horizontal red line across all columns, with a red dot placed on the left vertical axis.
- [x] 3.3 Style the current day column header highlight with a teal bottom border, colored day name and circle day number.
