## 1. Inicio Page Layout Spacing

- [x] 1.1 Remove `gap-gutter` from the `<main>` container in `frontend/app/inicio/page.tsx` and instead apply spaces below `PageHeader` at 1x gutter and spacing between internal sections at 1x gutter manually.

## 2. Agenda Mobile Header Duplication

- [x] 2.1 Wrap the compact system actions `<Header />` component in the calendar controls in `frontend/app/agenda/page.tsx` inside a `hidden md:block` element to prevent duplicate avatar on mobile.
